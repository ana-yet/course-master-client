/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ["placehold.co", "i.ibb.co.com", "campustechnology.com"],
  },
  reactCompiler: true,
};

export default nextConfig;
